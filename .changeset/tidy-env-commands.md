---
"@ma.vu/env": minor
---

Add the `mavu-env` CLI to validate shared schemas locally and in CI, with explicit JSON environment sources, multiple schemas, redacted failures, and validated Docker env-file export. Keep the core free of Node dependencies and automatic env loading. Export `EnvValidationError` and document backend, web and React Native workflows.
